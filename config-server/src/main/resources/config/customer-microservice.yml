spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: customer_db
      username: root
      password: pass
      authentication-database: admin

  output:
    ansi:
      enabled: ALWAYS

server:
  port: 8091